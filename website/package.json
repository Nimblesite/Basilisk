{
  "name": "basilisk-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Website for Basilisk.",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --watch",
    "clean": "rm -rf _site",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2",
    "eleventy-plugin-techdoc": "^0.2.0"
  }
}
