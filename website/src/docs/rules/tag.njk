---
pagination:
  data: ruleTagGroups.pages
  size: 1
  alias: ruleGroup
  addAllPagesToCollections: true
permalink: "/docs/rules/{{ ruleGroup.provenance }}/{{ ruleGroup.id }}/"
layout: layouts/docs.njk
eleventyComputed:
  title: "{{ ruleGroup.label }} rules — Basilisk"
  description: "Browse {{ ruleGroup.count }} Basilisk diagnostic rules tagged {{ ruleGroup.tag }}, with a dedicated explanation and canonical page for every code."
---
{% from "components/rules.njk" import groupPage with context %}
{{ groupPage(ruleGroup, "en") }}
