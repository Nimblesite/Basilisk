---
layout: layouts/base.njk
title: "Basilisk — Open-Source Python Language Server, Strict by Default"
description: "Open-source Python language server in Rust — strict-by-default checking, autocomplete, refactoring, debugging & profiling in VS Code, Cursor, Zed & Neovim."
permalink: /
# Translatable prose for the shared benchmark macro (_includes/benchmark-section.njk).
# The numbers come from _data/benchmarks.js (the git-tracked CSV); only the words live here.
benchmarkStrings:
  label: "Performance benchmarks"
  heading: "This is the starting line.<br>We&rsquo;re closing on Pyrefly."
  subhead: "These are today&rsquo;s numbers &mdash; produced by <code>make bench</code> and committed to the repo, so they move as we optimize. Basilisk already beats Pyright on every rule we benchmark; our target is Pyrefly-class throughput. These are <em>cold</em> full-file runs measured end-to-end; warm edits re-check only the file you touched and its importers, so they do strictly less work."
  ruleHeader: "Rule"
  versionsLabel: "Versions benchmarked"
  metaPre: "Mean wall-clock over "
  metaMid: " hyperfine runs; lower is better, ✓ = fastest. Measured on "
  metaCpuOpen: " ("
  metaOsSep: ", "
  metaCoresClose: " cores) on "
  metaDateClose: ". Source: "
  metaEnd: " &mdash; regenerated by <code>make bench</code>."
---

<script src="/assets/js/demo.js" defer></script>

<!-- ═══════════════════════════════════════════════════════
     HERO — left text / right code, no glows, no grid
═══════════════════════════════════════════════════════ -->
<section class="hero">
  <div class="container">
    <div class="hero__split">

      <div class="hero__text">
        <span class="hero__kicker">// basilisk — alpha</span>

        <h1 class="hero__headline">
          The open-source<br>
          Python language server.
        </h1>

        <p class="hero__subheadline">
          Basilisk is a complete, open-source Python language server.
          Autocomplete, go-to-definition, hover info, refactoring,
          diagnostics, integrated debugging &mdash; everything you rely on,
          without the proprietary lock-in. In VS Code, Cursor, Zed, and Neovim
          &mdash; wherever you write Python. Strict by default. Built in Rust.
        </p>

        <div class="hero__cta-group">
          <a href="/docs/installation/" class="btn btn--primary">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            Get Started
          </a>
          <a href="https://marketplace.visualstudio.com/items?itemName=Nimblesite.basilisk" class="btn btn--mono" target="_blank" rel="noopener">
            <span>Install for VS Code</span>
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
          </a>
          <a href="https://github.com/Nimblesite/Basilisk" class="btn btn--secondary" target="_blank" rel="noopener">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.477 2 2 6.477 2 12c0 4.42 2.865 8.166 6.839 9.489.5.092.682-.217.682-.482 0-.237-.008-.866-.013-1.7-2.782.603-3.369-1.342-3.369-1.342-.454-1.155-1.11-1.462-1.11-1.462-.908-.62.069-.608.069-.608 1.003.07 1.531 1.03 1.531 1.03.892 1.529 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.11-4.555-4.943 0-1.091.39-1.984 1.029-2.683-.103-.253-.446-1.27.098-2.647 0 0 .84-.269 2.75 1.025A9.578 9.578 0 0112 6.836c.85.004 1.705.114 2.504.336 1.909-1.294 2.747-1.025 2.747-1.025.546 1.377.202 2.394.1 2.647.64.699 1.028 1.592 1.028 2.683 0 3.842-2.339 4.687-4.566 4.935.359.309.678.919.678 1.852 0 1.336-.012 2.415-.012 2.741 0 .267.18.578.688.48C19.138 20.163 22 16.418 22 12c0-5.523-4.477-10-10-10z"/></svg>
            GitHub
          </a>
        </div>

        <div class="hero__meta">
          <span>Full <strong>LSP</strong></span>
          <span>Built in <strong>Rust</strong></span>
          <span><strong>Incremental</strong> re-checks</span>
          <span>Every IDE &mdash; <strong>VS Code</strong>, <strong>Cursor</strong>, <strong>Windsurf</strong>, <strong>Zed</strong>, <strong>Neovim</strong></span>
          <span><strong>MIT</strong> License</span>
        </div>
      </div>

      <!-- Product screenshot — Basilisk running in VS Code -->
      <div class="hero__code">
        <img src="/assets/images/vscode-diagnostics.png" alt="Basilisk flagging type errors live in VS Code — inline diagnostics with the Problems panel listing each missing annotation" class="hero__shot" loading="lazy">
      </div>

    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════
     PROBLEM — blunt text, no cards
═══════════════════════════════════════════════════════ -->
<section class="problem">
  <div class="container">
    <p class="problem__label">The problem</p>
    <div class="problem__grid">

      <div class="problem__statement">
        Python&rsquo;s default tooling is locked inside one proprietary VS Code extension. Python deserves a fully open-source language server that does <em>everything</em> &mdash; in <em>every</em> editor.
      </div>

      <ul class="problem__bullets">
        <li>
          <span class="problem__num">01</span>
          <span><strong>The most-used Python tooling is locked to one editor.</strong> The most popular Python extension is proprietary, and its richest features don&rsquo;t leave VS Code. Basilisk is MIT/Apache-2.0 and runs the same everywhere &mdash; VS Code, Cursor, Windsurf, Zed, Neovim &mdash; so the editor is your choice, not a constraint.</span>
        </li>
        <li>
          <span class="problem__num">02</span>
          <span><strong>Pyright is only a type checker.</strong> No autocomplete, no hover, no refactoring &mdash; not without Pylance wrapping it. Basilisk is a complete LSP: diagnostics, completions, go-to-definition, inlay hints, and more.</span>
        </li>
        <li>
          <span class="problem__num">03</span>
          <span><strong>Every type checker defaults to permissive.</strong> You get type checking only where you add <code>--strict</code>. Basilisk is strict by default &mdash; no opt-in required.</span>
        </li>
        <li>
          <span class="problem__num">04</span>
          <span><strong>Node.js is not a Python dependency.</strong> Pyright requires Node.js. Basilisk is a single Rust binary &mdash; no runtime, no npm, instant startup.</span>
        </li>
      </ul>

    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════
     FEATURES — alternating split rows
═══════════════════════════════════════════════════════════ -->
<section class="features">

  <div class="feature-row">
    <div class="feature-row__text">
      <p class="feature-row__num">01 &mdash; Strict by default</p>
      <h2 class="feature-row__title">Strict from line one.<br>Yours to dial down.</h2>
      <p class="feature-row__body">
        Basilisk treats every untyped function as an error &mdash; because that&rsquo;s what it is.
        But you stay in control: relax rules per-file or per-path from the editor UI or your
        config. And one-click enrichment fixes add the missing types <em>for</em> you.
      </p>
    </div>
    <div class="feature-row__visual">
      <div class="stat-block">
        <span class="stat-block__number">{{ ruleStats.total }}</span>
        <span class="stat-block__label">rules enabled, zero config</span>
      </div>
    </div>
  </div>

  <div class="feature-row">
    <div class="feature-row__visual">
      <div class="stat-block">
        <span class="stat-block__number">10<span class="stat-block__unit">/10</span></span>
        <span class="stat-block__label">benchmarked rules beat Pyright</span>
      </div>
    </div>
    <div class="feature-row__text">
      <p class="feature-row__num">02 &mdash; Measured, not promised</p>
      <h2 class="feature-row__title">Faster than Pyright.<br>Every rule we benchmark.</h2>
      <p class="feature-row__body">
        Every release runs <code>make bench</code> &mdash; hyperfine wall-clock against Pyright,
        mypy, ty, and Pyrefly on identical fixtures, with the numbers committed to the repo. On
        the rules we benchmark, Basilisk&rsquo;s cold full-file checks beat Pyright outright. And
        because re-checks are incremental &mdash; only the edited file and its importers are
        re-analyzed &mdash; warm edits do even less work.
      </p>
    </div>
  </div>

  <div class="feature-row">
    <div class="feature-row__text">
      <p class="feature-row__num">03 &mdash; Quick fixes &amp; refactoring</p>
      <h2 class="feature-row__title">Lightbulb-ready.<br>Every diagnostic.</h2>
      <p class="feature-row__body">
        Many diagnostics ship with a one-keystroke fix &mdash; add the missing annotation,
        modernise <code>Optional[X]</code> to <code>X | None</code>, convert to an f-string.
        Plus a full refactoring suite: extract, inline, change signature, and move symbol.
      </p>
    </div>
    <div class="feature-row__visual">
      <div class="code-window" style="width:100%;max-width:400px">
        <div class="code-window__chrome">
          <span class="code-window__dot code-window__dot--close"></span>
          <span class="code-window__dot code-window__dot--min"></span>
          <span class="code-window__dot code-window__dot--expand"></span>
          <span class="code-window__filename">quick-fix.py</span>
        </div>
        <div class="code-window__body">
<pre><span class="tok-cmt"># 💡 Quick Fix available</span>
<span class="tok-kw">def</span> <span class="tok-fn">process</span>(data) <span class="tok-cmt"># BSK-E0001</span>

<span class="tok-cmt"># → Add type annotation</span>
<span class="tok-kw">def</span> <span class="tok-fn">process</span>(data<span class="tok-type">: str</span>) <span class="tok-kw">-></span> <span class="tok-type">str</span>:
  <span class="tok-kw">return</span> data.upper()</pre>
        </div>
      </div>
    </div>
  </div>

  <div class="feature-row">
    <div class="feature-row__visual">
      <div class="stat-block">
        <span class="stat-block__number">{{ conformance.scorePct }}<span class="stat-block__unit">%</span></span>
        <span class="stat-block__label">PEP conformance score</span>
      </div>
    </div>
    <div class="feature-row__text">
      <p class="feature-row__num">04 &mdash; PEP conformance</p>
      <h2 class="feature-row__title">{{ conformance.pass }} of {{ conformance.total }} tests passing ({{ conformance.scorePct }}%).<br>Target: 100%.</h2>
      <p class="feature-row__body">
        Scored by the <a href="https://github.com/python/typing/blob/main/conformance/results/results.html">official Python typing conformance suite</a> &mdash;
        the same harness used to measure Pyright (~99%<a href="#citations" style="color:var(--color-text-muted);font-size:0.75rem">&sup1;</a>),
        mypy (~58%<a href="#citations" style="color:var(--color-text-muted);font-size:0.75rem">&sup1;</a>),
        and Pyrefly (~86%<a href="#citations" style="color:var(--color-text-muted);font-size:0.75rem">&sup1;</a>).
        Today {{ conformance.categoriesPass100 }} of {{ conformance.categoriesTotal }} categories pass at 100%; the suite reports {{ conformance.fp }} false-positive diagnostics and {{ conformance.missed }} missed required errors, and we are driving both to zero. 100% is the target, not a present-day claim. <a href="/docs/conformance/">How we measure this &rarr;</a>
      </p>
    </div>
  </div>

  <div class="feature-row">
    <div class="feature-row__visual">
      <div class="code-window" style="width:100%;max-width:400px">
        <div class="code-window__chrome">
          <span class="code-window__dot code-window__dot--close"></span>
          <span class="code-window__dot code-window__dot--min"></span>
          <span class="code-window__dot code-window__dot--expand"></span>
          <span class="code-window__filename">Basilisk Output</span>
        </div>
        <div class="code-window__body">
<pre><span class="tok-cmt">[Basilisk Debug]</span> createDebugAdapterDescriptor
  type=<span class="tok-type">basilisk-debug</span>, request=<span class="tok-str">launch</span>

<span class="tok-cmt">[Basilisk Debug]</span> Requesting LSP to spawn
  debugpy (python: <span class="tok-fn">auto-detect</span>)...

<span class="tok-cmt">[Basilisk Debug]</span> LSP spawned debugpy &rarr;
  connecting DAP client to
  <span class="tok-type">localhost</span>:<span class="tok-num">57356</span></pre>
        </div>
      </div>
    </div>
    <div class="feature-row__text">
      <p class="feature-row__num">05 &mdash; Integrated debugging</p>
      <h2 class="feature-row__title">F5 to debug.<br>Basilisk handles the rest.</h2>
      <p class="feature-row__body">
        Set breakpoints, step through code, inspect variables &mdash; all
        brokered through the Basilisk LSP. The Rust server spawns debugpy on a free
        TCP port and connects VS Code&rsquo;s DAP client automatically. No manual
        configuration. No separate debug extension. Just press F5.
      </p>
    </div>
  </div>

  <div class="feature-row">
    <div class="feature-row__text">
      <p class="feature-row__num">06 &mdash; Single binary</p>
      <h2 class="feature-row__title">Drop it in PATH.<br>That&rsquo;s the whole install.</h2>
      <p class="feature-row__body">
        Compiled to a standalone Rust binary. No Node.js. No Python interpreter.
        No <code>pip install</code>. No <code>npm install</code>.
        Works on macOS, Linux, and Windows from a single download.
      </p>
    </div>
    <div class="feature-row__visual">
      <div class="code-window" style="width:100%;max-width:400px">
        <div class="code-window__chrome">
          <span class="code-window__dot code-window__dot--close"></span>
          <span class="code-window__dot code-window__dot--min"></span>
          <span class="code-window__dot code-window__dot--expand"></span>
          <span class="code-window__filename">terminal</span>
        </div>
        <div class="code-window__body">
<pre><span class="tok-cmt"># macOS / Linux (Homebrew)</span>
<span class="tok-fn">brew</span> install Nimblesite/tap/basilisk

<span class="tok-cmt"># Windows (Scoop)</span>
<span class="tok-fn">scoop</span> install basilisk

<span class="tok-cmt"># then just run it</span>
<span class="tok-fn">basilisk</span> check src/</pre>
        </div>
      </div>
    </div>
  </div>

</section>

<!-- ═══════════════════════════════════════════════════
     COMPARISON TABLE
═══════════════════════════════════════════════════ -->
<section class="comparison-section">
  <div class="container">
    <p class="section-label">How it compares</p>
    <h2 class="section-heading">Everything you expect.<br>Fully open source.</h2>
    <p class="section-subhead">
      A feature-complete, open-source language server &mdash;
      with stricter type checking and no proprietary lock-in.
    </p>

    <div class="table-wrapper">
      <table class="comparison-table">
        <thead>
          <tr>
            <th>Feature</th>
            <th class="col-basilisk">Basilisk</th>
            <th>Pyright</th>
            <th>mypy</th>
            <th>ty</th>
            <th>Pyrefly</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Strict by default</td>
            <td class="col-basilisk check-yes">&#10003;</td>
            <td><span class="check-partial">opt-in</span></td>
            <td><span class="check-partial">opt-in</span></td>
            <td><span class="check-partial">opt-in</span></td>
            <td><span class="check-partial">opt-in</span></td>
          </tr>
          <tr>
            <td>PEP conformance <a href="#citations" style="color:var(--color-text-muted);font-size:0.75rem">&sup1;</a></td>
            <td class="col-basilisk">{{ conformance.scorePct }}% <em style="font-size:0.75rem;color:var(--color-text-muted)">({{ conformance.pass }}/{{ conformance.total }}, target 100%)</em></td>
            <td>~99%</td>
            <td>~58% full-pass</td>
            <td>alpha</td>
            <td>~86%</td>
          </tr>
          <tr>
            <td>Implementation</td>
            <td class="col-basilisk">Rust</td>
            <td><span class="cell-warn">TypeScript</span></td>
            <td><span class="cell-warn">Python/C</span></td>
            <td>Rust</td>
            <td>Rust</td>
          </tr>
          <tr>
            <td>Runtime required</td>
            <td class="col-basilisk check-yes">None</td>
            <td><span class="cell-warn">Node.js</span></td>
            <td><span class="cell-warn">Python</span></td>
            <td class="check-yes">None</td>
            <td class="check-yes">None</td>
          </tr>
          <tr>
            <td>Plugin system</td>
            <td class="col-basilisk"><span class="check-partial">WASM (planned)</span></td>
            <td class="check-no">&mdash;</td>
            <td><span class="check-partial">Python hooks</span></td>
            <td><span class="check-partial">Planned</span></td>
            <td class="check-no">&mdash;</td>
          </tr>
          <tr>
            <td>Full LSP (completions, hover, goto)</td>
            <td class="col-basilisk check-yes">&#10003;</td>
            <td><span class="cell-warn">Pylance only</span></td>
            <td class="check-no">&mdash;</td>
            <td><span class="check-partial">Basic</span></td>
            <td><span class="check-partial">Basic</span></td>
          </tr>
          <tr>
            <td>Integrated debugger</td>
            <td class="col-basilisk check-yes">&#10003;</td>
            <td class="check-no">&mdash;</td>
            <td class="check-no">&mdash;</td>
            <td class="check-no">&mdash;</td>
            <td class="check-no">&mdash;</td>
          </tr>
          <tr>
            <td>Editor extensions</td>
            <td class="col-basilisk">VS Code, Cursor, Zed, Neovim <em style="font-size:0.75rem;color:var(--color-text-muted)">(JetBrains soon)</em></td>
            <td><span class="cell-warn">Proprietary (Pylance)</span></td>
            <td class="check-no">&mdash;</td>
            <td>VS Code</td>
            <td>VS Code</td>
          </tr>
          <tr>
            <td>License</td>
            <td class="col-basilisk">MIT</td>
            <td>MIT</td>
            <td>MIT</td>
            <td>MIT</td>
            <td>MIT</td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════
     BENCHMARKING TABLE
═══════════════════════════════════════════════ -->
{% import "benchmark-section.njk" as benchmark %}
{{ benchmark.section(benchmarks, benchmarkStrings) }}

<!-- ═══════════════════════════════════════════════
     INTERACTIVE DEMO
═══════════════════════════════════════════════ -->
<section class="demo-section">
  <div class="container">
    <p class="section-label">Real diagnostics</p>
    <h2 class="section-heading">Basilisk in 30 seconds</h2>
    <p class="section-subhead">
      Real error codes. Every fix suggested inline. rustc-style output.
    </p>

    <div style="max-width:860px">
      <div class="demo-tabs">
        <button class="demo-tab active" data-tab="tab-missing">Missing types</button>
        <button class="demo-tab" data-tab="tab-typesafety">Type safety</button>
        <button class="demo-tab" data-tab="tab-clean">Clean code</button>
      </div>
      <div class="demo-wrapper">

        <div class="demo-panel active" data-panel="tab-missing">
          <div class="code-window">
            <div class="code-window__chrome">
              <span class="code-window__dot code-window__dot--close"></span>
              <span class="code-window__dot code-window__dot--min"></span>
              <span class="code-window__dot code-window__dot--expand"></span>
              <span class="code-window__filename">api.py</span>
            </div>
            <div class="code-window__body">
<pre><span class="tok-kw">from</span> <span class="tok-fn">typing</span> <span class="tok-kw">import</span> Any

<span class="tok-kw">def</span> <span class="tok-fn">fetch_user</span>(user_id):          <span class="tok-cmt">  # BSK-E0001, BSK-E0002</span>
    <span class="tok-kw">return</span> database.query(user_id)

<span class="tok-kw">def</span> <span class="tok-fn">save_record</span>(data: Any) <span class="tok-kw">-></span> <span class="tok-type">bool</span>:  <span class="tok-cmt"> # returns_compatibility (warning)</span>
    <span class="tok-kw">return</span> <span class="tok-kw">True</span></pre>
            </div>
          </div>
          <div class="diagnostic-block">
<span class="diag-prefix">error</span><span class="diag-bar">[</span><span class="diag-code">BSK-E0001</span><span class="diag-bar">]: </span><span class="diag-msg">Missing parameter type annotation for `user_id`</span>
<span class="diag-bar">  --> </span><span class="diag-path">api.py:3:16</span>
<span class="diag-bar">   |</span>
<span class="diag-bar"> 3 | </span><span class="diag-src">def fetch_user(user_id):</span>
<span class="diag-bar">   |                </span><span class="diag-caret">^^^^^^^</span>
<span class="diag-bar">   = </span><span class="diag-help">help: Add a type annotation: `user_id: &lt;type&gt;`</span>

<span class="diag-prefix">error</span><span class="diag-bar">[</span><span class="diag-code">BSK-E0002</span><span class="diag-bar">]: </span><span class="diag-msg">Missing return type annotation for function `fetch_user`</span>
<span class="diag-bar">  --> </span><span class="diag-path">api.py:3:5</span>
<span class="diag-bar">   = </span><span class="diag-help">help: Add a return type: `def fetch_user(...) -> &lt;type&gt;:`</span>

<span class="diag-prefix" style="color:var(--color-warning)">warning</span><span class="diag-bar">[</span><span class="diag-code">returns_compatibility</span><span class="diag-bar">]: </span><span class="diag-msg">Parameter `data` is annotated `Any` — prefer a concrete type</span>
<span class="diag-bar">  --> </span><span class="diag-path">api.py:6:17</span>
<span class="diag-bar">   |</span>
<span class="diag-bar"> 6 | </span><span class="diag-src">def save_record(data: Any) -> bool:</span>
<span class="diag-bar">   |                 </span><span class="diag-caret">^^^^</span>
<span class="diag-bar">   = </span><span class="diag-help">help: Replace `Any` on `data` with the actual expected type</span>
<span class="diag-bar">   = </span><span class="diag-see">see: https://www.basilisk-python.dev/errors/returns_compatibility</span>
          </div>
        </div>

        <div class="demo-panel" data-panel="tab-typesafety">
          <div class="code-window">
            <div class="code-window__chrome">
              <span class="code-window__dot code-window__dot--close"></span>
              <span class="code-window__dot code-window__dot--min"></span>
              <span class="code-window__dot code-window__dot--expand"></span>
              <span class="code-window__filename">totals.py</span>
            </div>
            <div class="code-window__body">
<pre><span class="tok-kw">def</span> <span class="tok-fn">get_count</span>(rows<span class="tok-type">: list[int]</span>) <span class="tok-kw">-></span> <span class="tok-type">int</span>:
<span class="code-error-line">    <span class="tok-kw">return</span> <span class="tok-str">"many"</span>  <span class="tok-cmt"># returns_compatibility_2: str is not int</span>
</span>
<span class="tok-kw">def</span> <span class="tok-fn">greet</span>(name<span class="tok-type">: str</span>) <span class="tok-kw">-></span> <span class="tok-type">str</span>:
    <span class="tok-kw">return</span> <span class="tok-str">f"Hello, {name}"</span>

<span class="code-error-line">greet(<span class="tok-num">42</span>)  <span class="tok-cmt"># calls_argument_type: int is not str</span></span></pre>
            </div>
          </div>
          <div class="diagnostic-block">
<span class="diag-prefix">error</span><span class="diag-bar">[</span><span class="diag-code">returns_compatibility_2</span><span class="diag-bar">]: </span><span class="diag-msg">Return type mismatch</span>
<span class="diag-bar">  --> </span><span class="diag-path">totals.py:2:12</span>
<span class="diag-bar">   |</span>
<span class="diag-bar"> 2 | </span><span class="diag-src">    return "many"</span>
<span class="diag-bar">   |            </span><span class="diag-caret">^^^^^^</span> <span class="diag-note">`str` is not assignable to declared return type `int`</span>
<span class="diag-bar">   |</span>
<span class="diag-bar">   = </span><span class="diag-help">help: return an `int`, or change the return annotation to `str`</span>
<span class="diag-bar">   = </span><span class="diag-see">see: https://www.basilisk-python.dev/docs/rules/type-safety/#returns_compatibility_2</span>
          </div>
        </div>

        <div class="demo-panel" data-panel="tab-clean">
          <div class="code-window">
            <div class="code-window__chrome">
              <span class="code-window__dot code-window__dot--close"></span>
              <span class="code-window__dot code-window__dot--min"></span>
              <span class="code-window__dot code-window__dot--expand"></span>
              <span class="code-window__filename">service.py</span>
            </div>
            <div class="code-window__body">
<pre><span class="tok-kw">from</span> <span class="tok-fn">dataclasses</span> <span class="tok-kw">import</span> dataclass

<span class="tok-dec">@dataclass</span>(frozen=<span class="tok-fn">True</span>)
<span class="tok-kw">class</span> <span class="tok-fn">Config</span>:
    host<span class="tok-type">: str</span>
    port<span class="tok-type">: int</span>
    debug<span class="tok-type">: bool</span> <span class="tok-kw">=</span> <span class="tok-fn">False</span>

<span class="tok-kw">def</span> <span class="tok-fn">create_config</span>(
    host<span class="tok-type">: str</span>,
    port<span class="tok-type">: int</span>,
) <span class="tok-kw">-></span> <span class="tok-type">Config</span>:
    <span class="tok-kw">return</span> Config(host<span class="tok-kw">=</span>host, port<span class="tok-kw">=</span>port)</pre>
            </div>
          </div>
          <div class="diagnostic-block diagnostic-block--success">
<span class="diag-prefix" style="color:var(--color-success)">All checked. No issues found.</span>
<span class="diag-note">Checked 1 file &mdash; 0 errors, 0 warnings in 4ms.</span>
          </div>
        </div>

      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════
     EDITORS — supported editor integrations
═══════════════════════════════════════════════════════ -->
<section class="features">
  <div class="feature-row">
    <div class="feature-row__text">
      <p class="feature-row__num">07 &mdash; Every editor</p>
      <h2 class="feature-row__title">Beautiful Python.<br>Whatever you use.</h2>
      <p class="feature-row__body">
        The same language server, the same diagnostics, the same fixes &mdash;
        in VS Code today, plus first-class Zed and Neovim extensions. Cursor and
        Windsurf (via Open VSX) are coming very soon; JetBrains (IntelliJ / PyCharm) is on the way. The goal
        is a great Python experience even if you never open VS Code.
      </p>
    </div>
    <div class="feature-row__visual">
      <div class="code-window" style="width:100%;max-width:400px">
        <div class="code-window__chrome">
          <span class="code-window__dot code-window__dot--close"></span>
          <span class="code-window__dot code-window__dot--min"></span>
          <span class="code-window__dot code-window__dot--expand"></span>
          <span class="code-window__filename">Editor support</span>
        </div>
        <div class="code-window__body">
<pre><span class="tok-cmt"># VS Code &mdash; Marketplace</span>
Install from the <span class="tok-str">Marketplace</span>
<span class="tok-cmt"># Cursor / Windsurf &mdash; Open VSX (coming very soon)</span>

<span class="tok-cmt"># Neovim</span>
<span class="tok-fn">require</span>(<span class="tok-str">"basilisk"</span>).setup()

<span class="tok-cmt"># Zed</span>
Install from <span class="tok-str">Extensions</span>

<span class="tok-cmt"># JetBrains — coming soon</span></pre>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════
     CTA — horizontal split, no glowing card
═══════════════════════════════════════════════════════ -->
<section class="cta-section">
  <div class="container">
    <div class="cta-inner">
      <h2>Open source.<br>Strict by default.</h2>
      <div>
        <p>
          Basilisk is in alpha. The type checker, LSP server, and editor
          extensions are working today &mdash; autocomplete, go-to-definition,
          hover info, diagnostics, refactoring, F5 debugging, and profiling &mdash;
          in VS Code, Cursor, Windsurf, Zed, and Neovim. JetBrains is on the way.
          Open source. No proprietary lock-in. Early adopters shape the roadmap.
        </p>
        <div class="cta-inner__actions">
          <a href="/docs/" class="btn btn--primary">Read the Docs</a>
          <a href="https://github.com/Nimblesite/Basilisk" class="btn btn--secondary" target="_blank" rel="noopener">View on GitHub</a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════════════════
     CITATIONS
═══════════════════════════════════════════════════════ -->
<section class="citations-section" id="citations">
  <div class="container">
    <h3 class="citations-heading">Sources</h3>
    <ol class="citations-list">
      <li>Full-pass conformance scores from the <a href="https://github.com/python/typing/blob/main/conformance/results/results.html" target="_blank" rel="noopener">official python/typing conformance suite</a> (pyright 1.1.408, mypy 1.19.1, pyrefly 0.54.0). mypy&rsquo;s partial+pass score is 96.4%.</li>
      <li>Pyrefly throughput (1.85M LOC/sec) from <a href="https://pyrefly.org/" target="_blank" rel="noopener">pyrefly.org</a> (measured on 166-core Meta infrastructure).</li>
      <li>Performance-benchmark table: mean wall-clock over {{ benchmarks.meta.runsCount }} hyperfine runs of each tool&rsquo;s CLI against large single-rule fixtures, measured on {{ benchmarks.meta.cpu }} ({{ benchmarks.meta.os }}, {{ benchmarks.meta.arch }}, {{ benchmarks.meta.cores }} cores) on {{ benchmarks.meta.generated }}. Tool versions: {% for t in benchmarks.meta.toolVersions %}{{ t.tool }} {{ t.version }}{% if not loop.last %}, {% endif %}{% endfor %}. Source <code>benchmarks/status/{{ benchmarks.primary }}.csv</code>; reproduce with <code>make bench</code>. These are cold full-file runs and do not reflect warm incremental (LSP) performance.</li>
    </ol>
  </div>
</section>