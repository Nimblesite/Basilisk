---
layout: layouts/base.njk
permalink: /
noTranslation: true
eleventyComputed:
  title: "{{ withdrawal.title }}"
  description: "{{ withdrawal.line }}"
---

{#- Implements [WITHDRAWAL-COPY-FULL]. Every word comes from
    docs/specs/DOCS-WITHDRAWAL-MESSAGING-SPEC.md via _data/withdrawal.json,
    generated by scripts/gen_withdrawal_copy.py. Do not write copy here. -#}
<article class="notice">
  <h1>{{ withdrawal.title }}</h1>
  {% for paragraph in withdrawal.full %}
  <p>{{ paragraph | safe }}</p>
  {% endfor %}
</article>
