[package]
name        = "basilisk-canonical"
description = "Canonical symbol resolution — the one registry that answers what a Python expression refers to"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ruff_python_ast.workspace    = true
# Forward references (PEP 484) parse their string contents as a type
# expression — the sanctioned parser, never text inspection.
ruff_python_parser.workspace = true
ruff_text_size.workspace     = true
serde.workspace              = true
thiserror.workspace          = true
toml.workspace               = true
tracing.workspace            = true

[lints]
workspace = true
