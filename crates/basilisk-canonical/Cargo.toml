[package]
name        = "basilisk-canonical"
description = "Canonical symbol resolution — the one registry that answers what a Python expression refers to"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ruff_python_ast.workspace = true
serde.workspace           = true
toml.workspace            = true

[lints]
workspace = true
