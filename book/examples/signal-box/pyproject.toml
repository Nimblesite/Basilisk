[project]
name = "signal-box"
version = "0.1.0"
requires-python = ">=3.12"

[tool.basilisk]
python-version = "3.12"
include = ["src", "tests"]

[tool.basilisk.rules]
"BSK-0001" = "error"
"BSK-0002" = "error"
